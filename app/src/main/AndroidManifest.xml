<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="meaning.emoji.com.emojimeaning">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SmileyPeopleActivity"
            android:label="Smiley &amp; People"/>
        <activity android:name=".AnimalsNatureActivity"
            android:label="Animals &amp; Nature"/>
        <activity android:name=".FoodDrinkActivity"
            android:label="Food &amp; Drink"/>
        <activity android:name=".ActivitiesActivity"
            android:label="Activity"/>
        <activity android:name=".TravelPlacesActivity"
            android:label="Travel &amp; Places"/>
        <activity android:name=".ObjectsActivity"
            android:label="Objects"/>
        <activity android:name=".SymbolsActivity"
            android:label="Symbols"/>
        <activity android:name=".FlagsActivity"
            android:label="Flags"></activity>
    </application>

</manifest>